# auto_shimmer_animate example

Runnable example app for the `auto_shimmer_animate` package.

From this directory:

```sh
flutter run
```

To choose a specific device:

```sh
flutter run -d chrome
flutter run -d macos
flutter run -d f981c0b8
```
